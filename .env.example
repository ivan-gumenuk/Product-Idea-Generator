# Supabase (required for Auth + DB)
# Local: run `supabase start` then `supabase status -o env` — use API_URL, ANON_KEY, SERVICE_ROLE_KEY
# Hosted: Project Settings → API at supabase.com
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
# Required for unsubscribe-by-token API (bypasses RLS)
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# OpenAI (for LLM idea generation + scoring)
OPENAI_API_KEY=sk-...

# Resend (for email digest + unsubscribe link)
RESEND_API_KEY=re_...
FROM_EMAIL=digest@yourdomain.com

# Optional: Reddit API (if not using mock data)
# REDDIT_CLIENT_ID=...
# REDDIT_CLIENT_SECRET=...

# App URL (for unsubscribe links in emails)
NEXT_PUBLIC_APP_URL=http://localhost:3000
